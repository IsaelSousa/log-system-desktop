import styled from 'styled-components';

export const NavBarContainer = styled.div`
    height: 50px;
    width: 100%;
    background: #00497c;
    position: fixed;
    top: 0;

    display: flex;
    align-items: center;
    padding-left: 1rem;
`;